# EnPu core — Docker image (Phase 3 / issue #13).
# Scaffold only; full image definition lands with cloud dual-mode work.

FROM python:3.12-slim

WORKDIR /app

# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt
# COPY app ./app
# EXPOSE 8765
# CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8765"]

CMD ["python", "-c", "print('EnPu core Dockerfile is a scaffold. See issue #13.')"]
