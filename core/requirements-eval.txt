# Eval / accuracy baseline CI (issue #38 / #29)
# Extends CI deps with PaddleOCR for print_clear gate.
-r requirements-ci.txt

# Paddle CPU (Linux CI / local). If install fails, see https://www.paddlepaddle.org.cn/install/quick
paddlepaddle>=2.6.0,<3.1
paddleocr>=2.7,<3
