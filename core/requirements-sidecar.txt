# Sidecar / release build deps only (#14 / #81).
# No PaddleOCR — mock engine + OpenCV structure + music21 export.
# Dev full OCR: pip install -r requirements.txt
# CI unit tests: requirements-ci.txt (similar, plus pytest)

fastapi>=0.110,<1
uvicorn>=0.27,<1
python-multipart>=0.0.9
pydantic>=2.0,<3
pydantic-settings>=2.0,<3
Pillow>=10.0,<12
numpy>=1.24,<3
opencv-python-headless>=4.8,<5
setuptools>=65
music21>=9.1,<10
httpx>=0.27,<1
