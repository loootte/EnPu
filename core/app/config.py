"""Application configuration (placeholder — issue #2)."""

# Example (to be implemented):
# from pydantic_settings import BaseSettings
#
# class Settings(BaseSettings):
#     host: str = "127.0.0.1"
#     port: int = 8765
#     cors_origins: list[str] = ["*"]
#
# settings = Settings()
