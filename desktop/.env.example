# Copy to desktop/.env for local overrides (Vite loads VITE_* at build/dev time).
# Default matches scripts/start.ps1 and core uvicorn.

# EnPu recognition core base URL (no trailing slash)
VITE_ENPU_CORE_URL=http://127.0.0.1:8765
